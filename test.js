const cli = require('./cli.js');

let musicCollection = cli.MusicCollection;

let collection = new MusicCollection();

// test('test1 - Empty deliverers array should throw error.', () => {
//     expect(() => {collection.add("", "").toThrow("You have not yet assigned anyone to deliver pizzas.");
// });